# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# IDE
.github

# ── Runtime data (TUI — JSON stores, root-level by design) ──────────────────
quests.json
pomodoros.json
trophies.json

# ── Runtime data (Web — SQLite) ──────────────────────────────────────────────
*.db
*.db-shm
*.db-wal
*.db-journal

# ── Backups ──────────────────────────────────────────────────────────────────
data/backups/
!data/backups/.gitkeep

# ── Environment ──────────────────────────────────────────────────────────────
.env
