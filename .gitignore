# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# IDE
.github

# ── Runtime data (TUI — JSON stores) ─────────────────────────────────────────
data/tui/

# ── Runtime data (Web — SQLite) ──────────────────────────────────────────────
data/web/
*.db
*.db-shm
*.db-wal
*.db-journal

# ── Backups ──────────────────────────────────────────────────────────────────
data/backups/
!data/backups/.gitkeep
!data/.gitkeep

# ── Environment ──────────────────────────────────────────────────────────────
.env
